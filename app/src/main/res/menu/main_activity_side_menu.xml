<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

<!--The name of the items are used to determin their actions.-->
    <group android:id="@+id/md_menu_second_group">
        <!--<item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="ifRoom" /> -->
        <item
            android:icon="@drawable/ic_add_board"
            android:id="@+id/additional_board_menu_btn_addBoard"
            android:title="@string/add_board"
            app:showAsAction="never" />
        <item
            android:icon="@drawable/ic_log_out"
            android:id="@+id/additional_board_menu_btn_logOut"
            android:title="@string/logout"
            app:showAsAction="never" />
    </group>
</menu>